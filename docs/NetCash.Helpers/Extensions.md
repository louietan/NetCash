# Extensions class

```csharp
public class Extensions
```

## Public Members

| name | description |
| --- | --- |
| static [CreateAccrualTransactions](Extensions/CreateAccrualTransactions.md)(…) | Create accrual transactions between two accounts. (I really need some feedback on the naming and the description.) |
| static [GetOrMakeAccount](Extensions/GetOrMakeAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |
| static [GetOrMakeAssetAccount](Extensions/GetOrMakeAssetAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |
| static [GetOrMakeEquityAccount](Extensions/GetOrMakeEquityAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |
| static [GetOrMakeExpenseAccount](Extensions/GetOrMakeExpenseAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |
| static [GetOrMakeIncomeAccount](Extensions/GetOrMakeIncomeAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |
| static [GetOrMakeLiabilityAccount](Extensions/GetOrMakeLiabilityAccount.md)(…) | Gets or creates an account. (Parent accounts are created on-demand). |

## See Also

* namespace [NetCash.Helpers](../netcash.md)

<!-- DO NOT EDIT: generated by xmldocmd for netcash.dll -->
